# Snapshot - 22/08/2026

## 🎯 O que foi feito hoje
- Projeto criado do zero: Next.js 14 + TS + Tailwind + Prisma 6 + NextAuth v5
- Pesquisa de referências de design (Bloomberg Terminal, dashboards fintech dark 2026) → proposta aprovada pelo Marcelo
- Design system implementado: fundo petróleo escuro (`#0A0D0F`) + acento âmbar (`#E8A33D`), Space Grotesk (display) + JetBrains Mono (números com `tabular-nums`)
- Componentes base: Button, Card, Badge, Input/Select, StatNumber
- Painel interno completo: login por senha (cookie assinado HMAC via Web Crypto), configurações (ativos, capital, meta mensal, thresholds do semáforo), formulário de lançamento de trade com cálculo automático + modal de confirmação, dashboard com período fechado (diário/semanal/mensal/semestral/anual) + meta mensal + semáforo de saúde 🟢🟡🔴, edição de trade com log de correção (nunca sobrescreve, guarda antes/depois + motivo)
- Área do investidor: NextAuth v5 + `@auth/prisma-adapter` + Resend (magic link sem senha), onboarding (capital inicial + data de entrada), saldo composto vs linear lado a lado com toggle + gráfico
- Página pública trocada de dados mock pra queries reais do Prisma (win rate, retorno acumulado, curva de capital, capital total sob acompanhamento somando o saldo de cada investidor)
- Neon conectado (banco `neondb`, sa-east-1) e todas as migrations aplicadas
- API key da Resend criada direto no painel deles (chave "Copy Nomade Trader", só permissão de envio) e salva no `.env`
- **Bug real achado e corrigido durante teste com o Marcelo:** faltava `emailVerified` (+ `name`/`image`) no model `User` — o `@auth/prisma-adapter` exige esses campos; sem eles o primeiro login via magic link quebrava com erro 500. Só apareceu no clique real do link recebido por e-mail
- Fluxo testado de ponta a ponta no Chrome e com o Marcelo testando ao vivo: painel interno completo, magic link chegando e funcionando, onboarding, tela de saldo, página pública com números reais — tudo validado, dados de teste sempre apagados depois
- Git com commits pequenos ao longo do dia, tudo publicado em https://github.com/Marcelo210598/copy-nomade-trader

## 📝 Decisões técnicas importantes
- Prisma: instalou v7 por padrão (breaking changes recentes) → fixado em v6, mais compatível com `@auth/prisma-adapter`
- Auth do painel interno: cookie HMAC-SHA256 via Web Crypto (não Node `crypto`) pra funcionar tanto no middleware Edge quanto em server actions
- "Resultado em pontos" é o movimento de preço (independe de contratos); "resultado em $" multiplica por `valorPorPonto * contratos`
- Cálculo do trade é sempre recalculado no servidor (nunca confia no valor mostrado no formulário do cliente)
- Print do trade salva em `public/uploads` por enquanto (não funciona no Vercel — trocar por Vercel Blob antes do deploy)

## ⚠️ Problemas encontrados e soluções
- Problema: npm cache com arquivos root-owned, `npm install` falhando
  - Solução: cache alternativo no scratchpad (`npm_config_cache=...`), sem precisar de `sudo chown`
- Problema: `npx prisma dev` (Postgres local) não funciona sem Docker rodando
  - Solução: usamos direto o Neon real do Marcelo (connection string do projeto `neondb`)
- Problema: badge de Compra/Venda usava verde/vermelho e confundia com resultado do trade
  - Solução: badge de lado ficou neutro; cor de resultado só aparece no %/pontos

## 🔧 Configurações adicionadas
- `.env` local preenchido com `DATABASE_URL` real do Neon (nunca commitado — confirmado com `git check-ignore`)
- `ADMIN_PASSWORD=admin123` (dev) — trocar antes de produção
- Migration `20260822232630_init` aplicada no Neon

## 📊 Estado atual do projeto
- Design system: ✅ pronto e aprovado
- Página pública: ✅ com dados reais do Prisma
- Painel interno (login, config, lançamento, dashboard, edição): ✅ completo e testado
- Área do investidor (magic link, onboarding, saldo composto/linear): ✅ completo e testado com o Marcelo ao vivo

## 🚧 Próxima sessão (To-do)
- Roteiro original 100% concluído. Alinhar com o Marcelo os próximos passos: gestão de investidores pelo admin, domínio de e-mail próprio, deploy na Vercel, reativar upload de print

## 💡 Observações importantes
- Banco sempre limpo ao fim da sessão (nenhum trade ou investidor de teste sobrou)
- `ADMIN_SESSION_SECRET` e `AUTH_SECRET` no `.env` ainda são placeholders de dev — gerar valores fortes (`openssl rand -base64 32`) antes de produção
- `RESEND_API_KEY` configurada e testada, mas `EMAIL_FROM` usa o domínio de teste (`onboarding@resend.dev`) — só entrega pro e-mail cadastrado na conta Resend, trocar por domínio próprio antes de ter investidores reais
- Neon free tier hiberna por inatividade — primeira request depois de um tempo parado pode falhar, segunda tentativa sempre funciona
