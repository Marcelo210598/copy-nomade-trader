# Snapshot - 24/08/2026

## 🎯 O que foi feito hoje
- **Saldo/valores trocados de R$ pra US$**: o campo do trade sempre foi `resultadoDolar`, mas `formatarMoeda` formatava como BRL. Corrigido pra `currency: "USD"` (mantendo separador `pt-BR`: `US$ 1.234,50`). Afeta saldo do investidor, painel admin e página pública.
- **Bug crítico de login corrigido**: o Luiz (investidor real) tentou logar e caiu numa tela "Server error / problem with the server configuration". Investigado e achado: `EMAIL_FROM` usava o domínio de teste da Resend (`onboarding@resend.dev`), que só entrega e-mail pro próprio dono da conta Resend — pra qualquer outro destinatário a API recusa com 403. Login de investidor só funcionava pro Marcelo mesmo, não pra ninguém mais.
  - Fix: reaproveitado `meutrade.app`, domínio já verificado na conta Resend do Marcelo (de outro projeto). `EMAIL_FROM` trocado pra `Copy Nomade Trader <login@meutrade.app>`.
  - Validado direto na API da Resend (envio de teste pra `delivered@resend.dev`, endereço sandbox deles): aceito sem 403.
  - Deploy em produção feito com `vercel deploy --prod`.

## 📝 Decisões técnicas importantes
- `formatarMoeda` mantém locale `pt-BR` mesmo com `currency: "USD"` — símbolo de dólar, mas separador de milhar/decimal brasileiro, consistente com o resto do app.
- Domínio `meutrade.app` (de outro projeto do Marcelo) reaproveitado como remetente por ser urgente (Luiz inicia vendas do copytrade hoje). Domínio próprio do Copy Nômade Trader continua pendente, sem prazo.

## ⚠️ Problemas encontrados e soluções
- Problema: login de investidor (magic link) quebrado pra qualquer pessoa que não o Marcelo — tela "Server error" do NextAuth.
- Causa raiz: restrição da Resend — domínio de teste (`resend.dev`) só entrega pro dono da conta.
- Solução: trocar `EMAIL_FROM` pra usar domínio verificado (`meutrade.app`) em vez do domínio de teste.

## 🔧 Configurações adicionadas
- `EMAIL_FROM` atualizado no `.env` local e nas envs Production/Preview da Vercel: `Copy Nomade Trader <login@meutrade.app>`

## 📊 Estado atual do projeto
No ar em produção: https://copy-nomade-trader.vercel.app. Login de investidor agora funciona pra qualquer e-mail, não só o do Marcelo. Valores exibidos em dólar (US$) em vez de real.

## 🚧 Próxima sessão (To-do)
1. Confirmar com o Marcelo que o Luiz conseguiu logar e ver o saldo em dólar corretamente
2. Domínio de e-mail próprio do Copy Nômade Trader na Resend segue pendente (hoje usa `meutrade.app` emprestado)
3. Candidatos de sempre: gestão de investidores pelo admin, reativar upload de print, domínio customizado

## 💡 Observações importantes
- Se aparecer de novo a tela "Server error / problem with the server configuration" em qualquer fluxo de auth, suspeitar primeiro de erro não tratado no provider/adapter do NextAuth (é a página genérica de erro dele, não é claro sobre a causa real) — checar Resend, Prisma adapter e envs antes de qualquer outra coisa.
