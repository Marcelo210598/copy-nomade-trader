# Snapshot - 23/08/2026

## 🎯 O que foi feito hoje
- Bateria de testes completa em produção: login admin, configurações, lançamento de trade, edição de trade com log de correção, dashboard (meta/semáforo/período/curva), página pública, onboarding do investidor — todos ✅
- Confirmado com o Marcelo: meta mensal/semáforo de saúde só aparece no painel interno (`/admin`), nunca na página pública nem na área do investidor — comportamento correto, sem mudança de código
- **Bug de regra de negócio encontrado e corrigido**: um trade lançado no sistema ANTES do investidor confirmar a entrada (onboarding), mas do mesmo dia da data de início escolhida, estava contando indevidamente no saldo dele. Corrigido:
  - Novo campo `entradaConfirmadaEm DateTime?` no model `User` (timestamp exato do onboarding, separado de `dataInicio` que é a data escolhida livremente e pode ser retroativa)
  - Nova função `filtrarTradesDoInvestidor` em `src/lib/saldo-investidor.ts`: trades de dias depois de `dataInicio` sempre contam, de dias antes nunca contam; no MESMO dia de `dataInicio`, só desempata por `trade.criadoEm` vs `entradaConfirmadaEm` quando a confirmação foi feita naquele mesmo dia (entrada retroativa continua contando o dia inteiro)
  - Aplicado em `/investidor` (saldo individual) e `estatisticas-publicas.ts` (capital total sob acompanhamento na página pública)
  - Migration Prisma aplicada no Neon, build de produção testado local antes do deploy, deployado com `vercel deploy --prod`
- Diagnosticado (não era bug real) onboarding que parecia não ter persistido — era submit incompleto da sessão anterior; refeito, funcionou
- Limpeza de dados de teste do banco (trades, configs) preservando o `User` real do investidor

## 📝 Decisões técnicas importantes
- Contas de investidor já existentes (sem `entradaConfirmadaEm` preenchido) caem no fallback "conta tudo" — mesmo comportamento de antes, sem quebrar nada até o investidor refazer o onboarding
- Marcelo decidiu **não** resolver o domínio de e-mail próprio agora ("vai demorar") — magic link continua podendo cair no spam até lá

## ⚠️ Problemas encontrados e soluções
- Ver seção de decisões acima — o bug do saldo foi a única correção de código do dia

## 📊 Estado atual do projeto
Todas as 3 áreas continuam no ar e testadas: https://copy-nomade-trader.vercel.app. Regra de saldo do investidor agora reflete corretamente "o que aconteceu depois que eu entrei".

## 🚧 Próxima sessão (To-do)
1. Marcelo vai refazer o onboarding e testar a correção do saldo na prática (lançar trade de teste antes/depois de confirmar entrada)
2. Domínio de e-mail próprio segue pendente, sem prazo — só retomar quando ele sinalizar
3. Candidatos de sempre: gestão de investidores pelo admin, reativar upload de print, domínio customizado

## 💡 Observações importantes
- Banco limpo ao fim da sessão de novo (só o `User` real do Marcelo/investidor ficou)
- Se for testar o saldo de novo, lembrar que só faz sentido comparar timestamps quando `dataInicio` é o dia de hoje (entrada retroativa não usa a granularidade de hora)
